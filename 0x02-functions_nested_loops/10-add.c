#include "main.h"

/**
 * add - adds two integers and returns result
 * @a: to be added with b
 * @b: to be added with a
 *
 * Return: the result of the function
 */
int add(int a, int b)
{
	return (a + b);
}
